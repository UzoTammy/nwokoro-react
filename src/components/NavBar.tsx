import { HStack, Image, Text } from "@chakra-ui/react";

const NavBar = () => {
  return (
    <HStack boxSize={12}>
      <Image src="" />
      <Text>UN</Text>
    </HStack>
  );
};

export default NavBar;
